{
    "$schema": "https://openapi.vercel.sh/vercel.json",
    "version": 2,
    "installCommand": "pnpm install",
    "buildCommand": "cd packages/database && npx prisma generate --schema=./prisma/schema.prisma && pnpm build && cd ../.. && pnpm --filter @mpt/web build",
    "outputDirectory": "apps/web/.next",
    "framework": "nextjs"
}